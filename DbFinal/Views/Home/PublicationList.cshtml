
@model List<DbFinal.Models.usp_PublicationList_Result>
@{
    ViewBag.Title = "Yayınevi Listesi";
}


<h2>Yayınevi Listesi</h2>

<div class="row" style="padding-bottom: 10px;">
    <a href="/Home/CreatePublication" class="btn btn-success pull-right btn-sm">Yayınevi Ekle</a>
</div>

<div class="row">
    <div class="table-responsive">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th class="text-center">#</th>
                    <th class="text-center">Yayınevi Adı</th>
                    <th class="text-center" style="width: 240px;">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count() > 0)
                {
                    int sayac = 0;
                    foreach (var item in Model)
                    {
                        sayac++;
                        <tr>
                            <td class="text-center"><b>@sayac</b></td>
                            <td class="text-center">@item.publicationName </td>
                            <td class="text-center">
                                <a href="/Home/DeletePublication?id=@item.publicationId" class="btn btn-danger btn-sm">Sil</a>                      
                                <a href="/Home/EditPublication?id=@item.publicationId" class="btn btn-primary btn-sm">Düzenle</a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8">Kayıt bulunamadı</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


